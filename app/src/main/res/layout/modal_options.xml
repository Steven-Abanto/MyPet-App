<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">

    <TextView
        android:id="@+id/tvTitulo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textStyle="bold"
        android:textSize="20dp"
        android:layout_marginTop="15dp"
        android:layout_marginHorizontal="15dp"
        android:text="Opciones para"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/mbMarcar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="15dp"
        android:text="Marcar como realizado"
        android:backgroundTint="#00CEC8"
        app:icon="@drawable/ic_dog"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/mbEliminar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="15dp"
        android:text="Eliminar"
        android:backgroundTint="#79DB70"
        app:icon="@drawable/ic_dog"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/mbCancelar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="15dp"
        android:text="Cancelar"
        android:backgroundTint="#008FD1"
        app:icon="@drawable/ic_cat"/>

</LinearLayout>